# CipherKeep — Project Summary

هذا الملف يشرح المشروع بالكامل من الصفر. لو بدأت محادثة جديدة ومعك هذا الملف فقط (+ باقي `docs/`)، يجب أن تفهم المشروع كاملًا بلا أي حاجة لسجل محادثات سابق.

---

## 1) فكرة المشروع

**CipherKeep** منصة تجارية لحماية أدوات المطورين (سكربتات بايثون) وإدارة تراخيصها. المطوّر (المالك/Owner) يشفّر أدواته، يوزّعها على عملاء بكود تفعيل، ويتحكّم بمن يقدر يشغّلها وعلى كم جهاز، مع إمكانية الإلغاء أو التمديد أو الإيقاف الطارئ في أي وقت.

## 2) الهدف

مشروع بجودة هندسية طويلة المدى، لا "بوت يشتغل" فقط. يبدأ ببوتات تيليجرام (تشفير + إدارة)، ويتوسّع مستقبلًا (خارج النطاق الحالي) للوحة ويب وAPI عام. الفلسفة الحاكمة: لا نصمم القمة قبل الأساس — كل طبقة تُبنى وتُثبَّت حيًا قبل البناء فوقها.

## 3) البنية المعمارية — أربع طبقات، اتجاه اعتماد واحد

```
البوتات/الواجهات (Adapters)
        ↓ يستدعي فقط
CipherKeep Core          ← كل منطق الأعمال
        ↓ يستدعي فقط
Data Access Layer (DAL)   ← وصول قاعدة بيانات فقط
        ↓
Supabase (Postgres)       ← التخزين الدائم الوحيد
```

**قاعدة صارمة:** كل طبقة تستدعي فقط الطبقة التي تحتها مباشرة. الـAdapter ممنوع يوصل DAL مباشرة.

### Core (`cipherkeep_core/`)
كل منطق الأعمال، بمعزل تام عن أي تقنية خارجية (لا تيليجرام، لا SQL خام، لا معرفة بقناة الاتصال). يعتمد على واجهات مجرَّدة (`typing.Protocol`) لا تنفيذ فعلي — هذا يمكّن اختباره بمستودعات وهمية بالذاكرة (`fakes.py`) بمعزل تام عن الشبكة.

**الدوال العامة الرئيسية بـ`CipherKeepCore`:**
- `create_code()` — إنشاء كود ترخيص جديد
- `verify_code()` — تحقق شامل (صلاحية + حجز سلوت جهاز) — المسار الأصلي، لا يزال يعمل بلا تغيير
- `check_code_validity()` — تحقق صلاحية **فقط**، بلا حجز (مضافة لإصلاح R1)
- `register_device()` — حجز سلوت جهاز **فقط**، تُستدعى بعد نجاح فك تشفير (مضافة لإصلاح R1)
- `revoke_code()` / `extend_code()` — عمليات مشرف فردي، تفرض فحص ملكية (`moderator_id`)
- `admin_force_revoke()` / `admin_pause_all()` — مسار إداري يتجاوز فحص الملكية عمدًا (مضافة لإصلاح C1، Kill Switch)
- `register_moderator()` / `resolve_moderator()` — إدارة المشرفين (Phase 2، منفَّذة كودًا)

### DAL (`cipherkeep_dal/`)
تنفيذ فعلي ضد Supabase عبر PostgREST (مكتبة `requests` فقط، بلا `supabase-py`/`psycopg2` — لتفادي تبعيات تجميع أصلي على Termux/Pydroid). كل دالة SQL على Supabase تبدأ بادئة `ck_` (مثل `ck_create_code`, `ck_claim_device_slot`, `ck_revoke_code`).

### Adapters (`adapters/`)
نقل فقط، بلا قرار عمل:
- **`license_server.py`** — سيرفر Flask، نقطتا التحقق والإدارة
- **`encryptor.py`** — أداة تشفير محلية (3 أوضاع: باسورد، مفتاح ملف، سيرفر)
- **`encryption_bot.py`** — بوت تيليجرام للتشفير، عميل HTTP خالص لـ`license_server.py`

### Tools (`tools/`)
أدوات إثبات حي يدوية (تحتاج اتصال Supabase حقيقي): `live_proof_supabase.py`, `full_lifecycle_proof.py`, `concurrency_test_supabase.py`.

## 4) التشفير — كيف يعمل

`encryptor.py` يشفّر ملف `.py` بـAES-256-GCM، ينتج `.enc.py` (لانشر ذاتي التشغيل يفك تشفير نفسه بالذاكرة وقت التشغيل). ثلاث طرق لإدارة المفتاح:
1. **باسورد يدوي** (Scrypt) — يُطلب كل تشغيل
2. **مفتاح عشوائي** (`secret.key`) — يُقرأ من ملف مجاور
3. **سيرفر (تفعيل عن بعد)** — يتصل بـ`license_server.py` عبر `/verify`، لا يحتاج مكتبة `cryptography` عند العميل النهائي

**قيد بنيوي مقبول بوعي (غير قابل للإصلاح):** نموذج "فك تشفير محلي ثم تنفيذ" لا يمكن أن يكون آمنًا ضد مهاجم مصمِّم بيده الملف — أي شخص يملك صلاحية تشغيل الملف يملك صلاحية قراءة المصدر وقت التنفيذ. هذا مُقَرّ به صراحة، والحماية الفعلية هي "رفع تكلفة النسخ العرَضي"، لا حاجزًا مطلقًا.

## 5) الأمان — ملخص كل الإصلاحات المُطبَّقة (بعد المراجعة الأمنية الشاملة)

| المعرّف | المشكلة | الحل |
|---|---|---|
| R1 | استنزاف سلوتات الأجهزة بمعرفة نص الكود وحده، بلا مادة تشفير صحيحة | الحجز مشروط بنجاح فك التشفير فعليًا |
| C1 | Kill Switch لا يؤثر على الأكواد الحية بـSupabase | مسار إداري منفصل يتجاوز فحص الملكية |
| H1 | Oracle Leak (تمييز حالات فشل الكود) | رد `/verify` موحَّد تمامًا لكل حالات صلاحية الكود |
| H2 | `ADMIN_TOKEN` بلا تدوير | تدوير يدوي عبر `ADMIN_TOKENS_ROTATION` |
| H3 | Rate Limit يُصفَّر مع كل إعادة نشر | واجهة جاهزة، تخزين دائم مؤجَّل لـPhase 2 |
| H4 | كلمة مرور ضعيفة قابلة لكسر Offline | حد أدنى 12 حرفًا + تعقيد إلزامي وقت الإنشاء |
| H5 | ملفات مصدرية تبقى بعد التشفير ببوت التشفير | حذف فوري بعد النجاح |
| H6 | كود التفعيل مضمَّن بنص واضح باللانشر | إدخال يدوي + حفظ محلي |
| L1 | فشل الإشعار يُبتلَع صامتًا | تسجيل بـ`activity.log` |
| L2 | غياب فرض HTTPS ببوت التشفير | فرض `https://` إلزاميًا |
| #19 | تسرّب استثناء خام عند فشل اتصال Supabase حقيقي | معالج `backend_unavailable` موحَّد لكل نقاط الدخول |

**قيدان غير قابلين للإصلاح (مقبولان بوعي):** نموذج فك التشفير المحلي (C2)، و`CIPHERKEEP_MASTER_KEY` كنقطة فشل واحدة (C3).

## 6) سير العمل — Owner / Moderators / Customers (Phase 2، قيد البناء)

```
Owner (المالك)
    ↓ يضيف/يعطّل/يحذف
Moderators (المشرفون)
    ↓ ينشئون ويفعّلون الأكواد لـ
Customers (العملاء)
```

المالك لا يتعامل مع العملاء مباشرة. المشرفون هم من يديرون دورة حياة الأكواد لعملائهم. أول مهمة Phase 2: ترحيل أوامر الإدارة المتبقية (`/admin/list`, `/admin/revoke`, `/admin/pause_all`, `/admin/extend`) بالكامل لـSupabase، حذف `licenses.json` نهائيًا، ثم بناء نظام صلاحيات المشرفين فوق البنية الجاهزة أصلًا (`ModeratorRepository`, `revoke_code`/`extend_code` بفحص ملكية).

## 7) حدود المشروع الحالية (صراحة كاملة)

- **لا** لوحة ويب، **لا** Dashboard، **لا** API عام — خارج النطاق الحالي تمامًا
- **لا** نظام باقات/تسعير محسوم بعد
- **لا** حل نهائي لضعف بصمة الجهاز على أندرويد/Termux (بصمة "أفضل جهد" فقط)
- **لا** Rate Limiting دائم عبر إعادة النشر بعد (H3 جزئي)
- بوت الإدارة (Telegram Adapter لصلاحيات المشرفين) **لم يُكتب بعد** — البنية جاهزة بـCore/DAL فقط

## 8) إحصائيات حالية

- **71/71** اختبار آلي ناجح (26 Core + 11 ServerMode + 20 DAL + 14 Adapter)
- **15 ملفًا** عُدِّلت أثناء المراجعة الأمنية (صفر ملف جديد — كل الإضافات داخل ملفات قائمة)
- **Phase 1: مغلَقة رسميًا** (22/22 بند)
- **Phase 2: قيد البدء** (المهمة الأولى: ترحيل أوامر الإدارة)
